# REVIEW.md — Review Log (ORCH-only writes)

## Per-unit performance tallies

| Unit | Reviews | First-pass approvals | Rework | Common rework causes |
|---|---|---|---|---|
| GB | 5 | 5 | 0 | — |
| CX | 7 | 5 | 1 | §4 truncation-ladder test coverage (rungs 2–3 + episodic content untested) — cleared on re-review |
| S5 | 5 | 3 | 1 | Corrupt-db escalation unreachable (marker-match on head-truncated atlas.py traceback) — spec §5 safety carve-out unmet; also a prior worktree/main-checkout protocol violation on the same task. Cleared on re-review (direct sqlite probe). |

Evidence here refines assignment heuristics (protocol §8) after ~10 reviews.

## Verdicts

| Task | Unit | Verdict | Findings | First-pass | Timestamp |
|---|---|---|---|---|---|
| TASK-002 | CX | approved | Territory clean (5/5 in Owned_Paths, no PLAN.md edits). 618 Python + 30 core + 36 Node green, re-run by ORCH — matches Test_Evidence. §7 A1 exit criteria all pass on repo: full scan 1.41s (<10s), `where decide`→supervisor.py:224+callers, `impact builder_registry.py` lists budget.py+validate_plan.py, one-touch re-parses exactly 1, exit codes 0/1 never 2. Non-blocking: (1) query uses LIKE not FTS5 MATCH/bm25 (fast now, revisit at scale); (2) no ls/find check evidence in history (first-occurrence, all files net-new). | yes | 2026-08-13T16:07:16Z |
| TASK-004 | S5 | approved | Territory clean: net diff master...task/TASK-004-s5 = exactly the 3 Owned_Paths (docs/ATLAS.md, scripts/atlas_cards.py, tests/test_atlas_cards.py, all net-new); own commit a27da61 touched exactly those 3; legit merge 862c0e3 carried only master's already-landed TASK-003 files + PLAN.md forward, zero out-of-territory S5 edits; c8b9872 filesystem-check evidence present (16:30:30Z preflight); no S5 PLAN.md/frontmatter edits (PLAN.md byte-identical to master). Spec verified §1 Cards/§3/§6 A3/§7/§8 Q1/R1/R4 — all 7 ACs map to spec text; five card fields + one headless sonnet-4-6 call/changed-file + hash-pin + never-in-scan confirmed; fake-model-transcript test discipline confirmed (zero live calls). ORCH re-ran in worktree: 21 cards + 657 full Python + 36 Node green (matches Test_Evidence). §7 A3 exit criteria verified END-TO-END through atlas.py CLI (isolated temp repo, fake cards_cmd): regenerate-only-on-hash-change + doctored-hash FRESH→STALE flip via core.query, exit 0/1/never-2, R4 clean degradation (model unreachable → cards exit 1, scan/query/status exit 0). Non-blocking: S5 evidence stamps 16:45/16:46Z run ahead of the ~16:33Z actual commit clock (UTC-hygiene reminder, no correctness impact). Unlocks TASK-005 (CX) — Wave 2 complete. | yes | 2026-08-13T16:41:36Z |
| TASK-005 | CX | rework | Territory clean (net diff master...task/TASK-005-cx = exactly scripts/atlas_pack.py + tests/test_atlas_pack.py, both net-new; single commit 231031f, no merge, no PLAN.md/frontmatter edits; c8b9872 preflight present 16:49Z). Composer functionally CORRECT — ORCH verified §4 end-to-end incl. all 3 truncation rungs manually (fresh card: budget 520 drops episodic+neighborhood/keeps card body ≤520; 500 drops all 3 incl. card bodies ≤500; below floor → clean exit-1, never 2), real-repo pack = territory outline + head-fallback + pointer-only neighborhood (no bodies) + matched episodic hits + R1 footer verbatim. Suites green, ORCH re-run in worktree: 6 pack + 663 full Python + 36 Node (matches Test_Evidence). REWORK is a §4 hard-cap TEST-COVERAGE gap (brief flagged as rework-not-note): (1) truncation ladder tests only rung 1 (episodic drop) — rungs 2 (neighborhood) + 3 (card bodies) uncovered, need a fresh-card fixture to exercise; (2) episodic-hit CONTENT unverified (only the "## EPISODIC HITS" header asserted, which renders even when empty); (3) preflight_paths reuse cosmetic — describe() called+discarded, resolution reimplemented via glob (honesty-of-coupling fix, semantics already match). Non-blocking: outline lists locals as `const` (atlas_core, out of territory); em-dash mojibake is console-cp only. | yes | 2026-08-13T16:59:34Z |
| TASK-005 | CX | approved | RE-REVIEW after rework. All 3 prior findings fixed with real discriminating assertions: (1) truncation ladder now full — test_budget_drops_lowest_priority_sections_first (budget 160, fresh-card fixture) = rung 2 (episodic + one-hop neighborhood dropped, territory card BODY kept, tokens ≤160); test_budget_drops_territory_card_bodies_last (budget 150) = rung 3 (all three dropped, card body collapsed to short card, tokens ≤150); both assert the exact cumulative "Truncation: …" line. (2) Episodic CONTENT asserted — actual "TASK-010 (dossier): Widget pack previous rework evidence." line, not just the header. (3) preflight_paths dead call removed (import + discarded describe() gone), glob/file/dir rules kept with an honest comment; verified describe() uses the same "*?[" + recursive-glob classification. Territory clean: net diff master...task/TASK-005-cx = exactly scripts/atlas_pack.py + tests/test_atlas_pack.py (both net-new, CX commits 231031f + a521811, no merge, no PLAN.md/frontmatter edits). ORCH re-ran in worktree: 7 pack + 664 full Python + 36 Node green (matches Test_Evidence); all four target tests confirmed run+passed. Real-repo §4 spot-check intact: 4 sections + freshness footer + R1 verbatim + pointer-only neighborhood (no bodies) + tiny budget → clean exit 1 never 2 + JSON machine-readable. Merged --no-ff, branch deleted. Unlocks TASK-006 (S5, A5) — dispatch gated on PROTECTED_EXCEPTIONS grants for its all-protected Owned_Paths. | no | 2026-08-13T17:22:00Z |
| TASK-006 | S5 | rework | Territory CLEAN (net diff master...task/TASK-006-s5 = exactly the 10 Owned_Paths / 12 files, zero outside; two S5 commits 0878709+408d4d4; no PLAN.md commit on branch). Pre-review protocol violation remediated & CONFIRMED: final diff exists ONLY as branch commits, main checkout has no tracked modifications. autopilot.json read line-by-line = ONLY the atlas block appended (builders registry / review_cmd / judgment_model untouched). tests/test_maintenance.py = exactly the two `len(steps)==6→7` assertions. §7 byte-identical-when-disabled EMPIRICALLY verified (not "structurally"): same-cwd dispatch.ps1 -DryRun with db present + enabled=false `-ceq` the master-overlay run, both 2705 chars. §7 dry-run section render verified (enabled + eligible TASK-003 @3000 → `## PROJECT MAP (ATLAS)` section + R1 footer verbatim; disabled → absent; fail-open → one warning line, PROMPT untouched). Briefings ×3 carry R1 verbatim + query/where/impact CLIs (R3); decompose impact-prose, onboard ask-step + R2 gitignore, board_publisher optional atlas key (existence-checked). R2 confirmed (scan → gitignored db, no git status change). Suites re-run by ORCH in worktree: 664 pytest + 36 node + 49 maintenance green (matches Test_Evidence). **BLOCKING:** maintenance.py corrupt-db escalation is UNREACHABLE — `_step_atlas`/`_atlas_run` matches `_ATLAS_CORRUPTION_MARKERS` against `_oneline(...,400)` head-truncated output, but atlas.py crashes with an uncaught traceback on `sqlite3.DatabaseError` (except tuple omits sqlite3.Error) so the marker line is always past char 400; both corruption classes tested → passed=True / no escalation / no delete+rescan remedy → spec §5 + AC unmet (silent nightly rot). Fix in-territory (probe db via sqlite3 or scan untruncated output + regression test). Non-blocking: dispatch.ps1 header hyphen vs spec/dispatch.sh em-dash. PROTECTED_EXCEPTIONS grants remain LIVE for the rework; not merged, branch retained. | no | 2026-08-13T18:27:31Z |
| TASK-006 | S5 | approved | RE-REVIEW after rework. Blocking corrupt-db finding FIXED for real: `_atlas_db_corrupt(db)` (scripts/maintenance.py) probes `.devteam/atlas.db` directly via sqlite3 `PRAGMA integrity_check` (+ `sqlite3.DatabaseError` on a non-db garbage file), independent of atlas.py's head-truncated traceback; `_step_atlas` escalates on `marker_hit OR _atlas_db_corrupt(db)` — corrupt path now REACHABLE. Verified: corrupt-db → passed=False, remedy detail contains "delete"+"re-run"+"scan --full" (spec §5 delete+full-rescan); ordinary scan failure → passed=True log-only (fail-open kept); missing/valid db → not corrupt (no false escalation, probe only runs post scan-failure). Regression test DISCRIMINATING: test_corrupt_db_escalates_with_delete_and_rescan_remedy strips the marker from the tail (`assert "DatabaseError" not in truncated_tail`) so escalation can only come from the direct probe — exactly the finding's failure mode; + TestAtlasDbCorrupt ×3 + fail-open/ok-path. 4 target tests confirmed run+passed individually (worktree). ORCH re-ran in worktree: pytest 671/0 (664 + 7 new), node 36/0. Territory CLEAN: net diff = exactly 12 files / 10 Owned_Paths, zero outside; four S5 commits (0878709, 408d4d4, 46492e8, 10b50fd); no PLAN.md commit on branch; main checkout no tracked mods. §7 byte-identical-when-disabled holds: this pass's only dispatch.ps1 change (10b50fd) is a comment inside the atlas-enabled branch — disabled gate + PROMPT assembly untouched, prior empirical 2705==2705 stands. Cosmetic hyphen ADDRESSED non-gating (ASCII hyphen kept for PS 5.1 codepage safety + explanatory comment). Merged --no-ff, branch deleted. **ATLAS PLAN COMPLETE (5/5 tasks done+merged).** Follow-up: remove now-unneeded PROTECTED_EXCEPTIONS grants (hooks/lib.js fc27360) in a dedicated [ORCH] commit. | no | 2026-08-13T19:42:00Z |
| TASK-003 | GB | approved | Territory clean: own commit 289499a = exactly the 2 Owned_Paths; merge 6f61a81 carried only ORCH's master fix f938fa0 + PLAN.md forward, no GB out-of-territory edits; c8b9872 filesystem-check evidence present; no GB PLAN.md/frontmatter edits. Spec verified §1/§3/§6 A2/R4 — all 6 ACs map to spec text; parser reuse confirmed (parse_tasks/ROW_RE/parse_instincts, no duplicate). ORCH re-ran in worktree: 18 episode + 636 full Python + 36 Node green (matches Test_Evidence); real-repo smoke episode hits TASK-002:1 + TASK-003:1, forward-slash, exit codes correct. Non-blocking: incremental never converges to changed:0 — a present source that parses to zero episodes (INSTINCTS.md today) is never recorded in the episodes table, so it re-counts as changed and rebuilds episodes_fts every run (cosmetic/perf, no data impact, no AC violated); idempotency of a zero-yield source is also untested. Fast-follow. | yes | 2026-08-13T16:32:48Z |
| TASK-010 | S5 | approved | Territory CLEAN: net diff master...task/TASK-010-s5 = exactly `.gitattributes`, single S5 commit f2d7f9b, zero outside; no PLAN.md commit on branch (earlier stray-PLAN.md pickup caught+reset by S5 pre-submission); no frontmatter/other-block edits. c8b9872 preflight evidence present (18:40). Spec verified §4 (H-D) / §8 Q1 / §7 — all 5 ACs map to spec text; ORCH option-(b) decision (text=auto forward-only, NO mass renorm) implemented + recorded verbatim in the file's comments incl. the sync_from_pack.py byte-exactness interaction. EMPIRICAL (S5 worktree): `git add --renormalize .` stages ZERO changes — no existing tracked file's bytes change, no whitespace sweep (§8 Q1 b); re-confirmed empty on integrated master post-merge. `git check-attr` → PLAN.md/README.md text=set eol=lf (the `*.md eol=lf` rule after `* text=auto` wins as last match, pinning the working-tree line ending → warning stops); .sh/.py/.js retain eol=lf (shell LF correctness intact; text=set→auto changes no bytes, blobs already LF). Appending to PLAN.md + `git add` → NO CRLF warning (was always printed); restored clean. ORCH re-ran (subagent, worktree): test_sync_from_pack.py 44/0 (byte-exactness preserved), full pytest 697/0, node 36/0 — matches Test_Evidence (697 vs 698 on integrated master expected: branch predates TASK-009). Merge into current master (carries TASK-009) clean, no conflict — disjoint territories confirmed. No PROTECTED_EXCEPTIONS grant to remove (.gitattributes not firewall-protected). Merged --no-ff (b2cf9e1), branch deleted. | yes | 2026-08-16T19:20:00Z |
| TASK-009 | CX | approved | Territory CLEAN: net diff master...task/TASK-009-cx = exactly the 2 Owned_Paths (scripts/atlas_episodes.py, tests/test_atlas_episodes.py), single CX commit 0b75073, zero outside; no PLAN.md commit on branch; no frontmatter/other-block edits. Spec verified §3 C2 / §0 H3 / §7 — all 5 ACs map to spec text. C2 fix: per-source hash persisted in `meta` under `episodes_source_hash:` prefix for EVERY scanned source (recorded on both changed-insert and unchanged-skip paths); `_existing_source_hashes` merges meta rows with episodes-derived keys, so a present-but-zero-episode source (INSTINCTS.md) now has a persisted key and stops re-counting; `--reindex` clears the meta hashes with episodes/fts; `_delete_source` deletes the paired meta key (orphan cleanup consistent); unchanged-path re-record self-heals pre-fix DBs. EMPIRICAL (CX worktree): `episodes` twice → 2nd `sources changed: 0`; `--reindex` → `sources changed: 11` full rebuild then converges to 0; exit 0; §3 output shape unchanged. H3 DISCRIMINATING: master pre-fix source under branch test → test_incremental_records_hash_for_zero_episode_source FAILS (`assert stored is not None`→None); pins the INSTINCTS.md zero-episode case. ORCH re-ran (subagent, worktree): pytest 698/0, node 36/0 — matches Test_Evidence; named regression test run+passed. Non-blocking: no explicit ls/find filesystem-check (c8b9872) evidence, but both paths pre-existed (modifications) and 18:38 "Baseline inspected" note shows read-before-edit — no clobber risk; reminder to record preflight next time. Merged --no-ff (9db0c72), branch deleted; TASK-009 PROTECTED_EXCEPTIONS grant (scripts/atlas_episodes.py) removed from hooks/lib.js in the same [ORCH] commit. TASK-008 (GB) still in flight — its atlas_core.py/docs/ATLAS.md grants untouched. | yes | 2026-08-16T19:05:00Z |
| TASK-008 | GB | approved | Territory CLEAN: 3-dot diff master...task/TASK-008-gb = exactly the 3 Owned_Paths (scripts/atlas_core.py, tests/test_atlas_core.py, docs/ATLAS.md); own commit 5166f25 = exactly those three; legit master-merge 5d7d22c carried only already-on-master TASK-009 files + PLAN/REVIEW forward + TASK-009's hooks/lib.js grant removal — zero GB out-of-territory edits. c8b9872 preflight present (18:43:50Z, all three EXIST). No GB PLAN.md/frontmatter edits. Spec verified §1 H-A / §2 H-B / §3 C1 / §0 H1-H3 / §7 — all 8 ACs map to spec text. EMPIRICAL (independent ORCH + new-code runs): H-A — nested node_modules/ excluded, /dist/ root-anchored (misses packages/a/dist/) while dist/ matches, trailing-slash dir never matches a like-named FILE, /README.md anchors to root; 13 pre-existing parametrized ignore cases pass UNCHANGED (widening not redesign). H-A scan delta: old 120 vs new 120 files on identical main tree → delta 0 (nothing to drop/re-include here; 116→120 vs GB baseline is tree growth, not the widening — claim confirmed). H-B — REAL git worktree add: db_path(linked)==db_path(main)==<main>/.devteam/atlas.db; core.query + atlas.py query from the linked worktree return the MAIN index; no-git/non-worktree falls back to repo/.devteam/atlas.db without raising (R4); documented in docs/ATLAS.md. C1 — FTS5 MATCH+bm25 on files_fts+episodes_fts; terms double-quote-escaped AND wrapped in an OperationalError catch (double-guarded); hyphen/quote/colon/AND-OR/path:colon terms all no-raise, CLI exit 0 never 2; FRESH/STALE + output shape preserved; relevance ordering pinned (zzz_dense before aaa_rare). H3: named regression tests (gitignore ×12, nested-node_modules, db_path-main-checkout, db_path-fallback ×2, relevance-order, multiword-punctuation, fresh-stale) all executed+PASSED, 0 deselected. ORCH re-ran (subagent, worktree): pytest 717/0, node 36/0 — matches Test_Evidence exactly; full suite re-run on integrated master as the cross-package regression guard. CLOSES the PACK HARDENING wave (008+009+010 done; H-E remains ORCH). Merged --no-ff (9417cc1), branch deleted, worktree removed. Grant removal (atlas_core.py + docs/ATLAS.md, hooks/lib.js) left to Alister per standing arrangement; working tree showed NO pending grant deletion at review time. | yes | 2026-08-16T19:45:00Z |
| TASK-007 | GB | approved | Territory CLEAN: net diff master...task/TASK-007-gb = exactly the 3 Owned_Paths (scripts/atlas_core.py, tests/test_atlas_core.py, onboard.md), single GB commit 14d096b, zero outside; NO edits to atlas.py/atlas_cards.py/atlas_pack.py/dispatch.sh/.ps1/maintenance.py. onboard.md = the ONE granted line only (ATLAS ask-step cards question, gated on enabled, silence=neither) — within the deliberate ORCH grant, no other onboard edit. c8b9872 preflight evidence present (16:08:30Z, all three EXIST); no GB PLAN.md/frontmatter edits, no PLAN.md commit on branch. Spec verified §9 A6-1/A6-2/A6-3 + §3 invariants + R4 — all 7 ACs map to spec text. ORCH re-ran (opus): test_atlas_core.py 40 + full pytest 687 / 0 fail + node 36/0 — matches Test_Evidence exactly. §9 exit criteria EMPIRICAL on this repo: A6-1 last_scan_head = real HEAD w/ git, '' in non-git tree (direct sqlite read); A6-2 delta moves WITHOUT scan (111v111 in-sync → commit-no-scan 112v111 "1 not indexed" / commits 1) and RECONCILES after rescan (112v112 in-sync / commits 0), throwaway commit reset out (branch merged clean at 14d096b); R4 no-git dir → tracked/commits "n/a" at exit 0; cards opt-in hint verbatim on empty table; em-dash literal correct UTF-8. N filters git ls-files by scanner membership (ignore rules + TEXT_EXTENSIONS, scan skips non-text at atlas_core.py:218) so clean-tree reports "in sync" — faithful reading, documented in-code; ignore-rule + dot-path tests discriminating. Non-blocking (no rework): a git-tracked text-ext file skipped by the scanner for binary/non-UTF-8 content would read as "not indexed" — benign edge the repo never hits; future-ticket note only. Merged --no-ff (747bd80), branch deleted. Follow-ups (ORCH, dedicated commits): remove TASK-007 onboard.md PROTECTED_EXCEPTIONS grant; sync pack to oikonomos/orb-jun-26/rwc-admin-portal (pack READY — A6 is the whole point of this wave). | yes | 2026-08-15T16:24:00Z |
| TASK-011 | GB | approved | Territory CLEAN: net diff master...task/TASK-011-gb = exactly the 3 Owned_Paths (scripts/dispatch.sh, scripts/dispatch.ps1, tests/test_dispatch_worktree.py), single GB commit b5cd0e6, zero outside; no PLAN.md commit on branch; no frontmatter/other-block edits. c8b9872 preflight present (21:16:00Z, all three EXIST). Spec verified §2 (R-A) / §1 (L1, L4) / §5 / §6 — all 6 ACs map to spec text. Control flow re-derived on BOTH scripts: reclaim sits only inside the existing unregistered-directory guard; a registered worktree still reaches the reuse/refresh path (ps1) and the create path is reached after reclaim (bash `if [[ ! -d ]]`, ps second `if Test-Path…else create`); empty→reclaim, non-empty + dotfile-only→verbatim refusal, locked-removal→clean exit-1, never crash/partial. Safety: bash `ls -A`+`rmdir` (never rm -rf); ps `Get-ChildItem -Force`+non-recursive `Directory.Delete` — neither can wipe real work; exactly one reclaim log line each; behavioural parity test green. §6 (GRADED) SATISFIED — evidence shows the failures ACTUALLY occurring: ORCH independently ran TestEmptyHuskReclaim in the worktree 7/7, incl. the cwd-lock failed-removal case (ran, NOT skipped → lock-refusal genuinely exercised on Windows) + PS1 parity + dotfile-refusal. L4 verified EMPIRICALLY: swapped pre-fix master scripts under the new tests → empty-reclaim (sh) + PS1 twin both FAIL (exit-1 refusal, no "Reclaimed"); branch restored — genuine regressions, not tautologies. ORCH re-ran full suites in worktree (subagent): pytest 724/0, 0 skipped (717 baseline + 7), node 36/0 — matches Test_Evidence exactly (0 skipped confirms the lock test ran). Merged --no-ff (bc1aefb), branch deleted, wt-grok detached to master (reuse-ready). TASK-011 dispatch.sh + dispatch.ps1 firewall grants remain LIVE in hooks/lib.js — removal deferred to Alister's dedicated grant step per standing instruction; TASK-012 (CX) supervisor.py/autopilot.json grants untouched (in flight). | yes | 2026-08-16T21:30:24Z |
| TASK-015 | S5 | approved | Territory CLEAN: net diff master...task/TASK-015-s5 = exactly the 4 Owned_Paths (scripts/slack_notify.py, scripts/notify.py, tests/test_slack_notify.py, tests/test_notify.py); S5 commits 77edb2d+aaa0ed9 + one legit ORCH master-merge caa00d4 (sync-manifest fix, no S5 out-of-territory edits); no PLAN.md/frontmatter edits on branch; c8b9872 preflight present (04:03:30Z, all four paths). All 8 ACs verified against SLACK spec text itself (not the builder summary): §5 Web API — chat.postMessage(blocks=)/chat.update/reactions.add via stdlib urllib, NOT an incoming webhook; token DEVTEAM_SLACK_TOKEN env only (§8), never a tracked file; all four §3 Block Kit designs with exact button rows (P2-BLOCKED Approve/Rework/Answer, P2-NEEDS-REVIEW Open-Tower/Approve/Rework, P1-STOP-THE-LINE Resume, P0-WAVE-COMPLETE View-PLAN/Open-Tower/Full-stats); §2 routing EXACT (P0/P1→ops_channel, P2/status/usage→project_channel, wave_complete→both deduped); thread tracking via .devteam/slack_threads.json keyed by task_id (post→thread_ts reply→chat.update in place on decision→✅ reaction on done; atomic tmp+os.replace write); 429 exponential backoff honouring Retry-After header + fail-open never-raises single chokepoint (§5); --test smoke subcommand posts one msg/configured channel + reports delivery (§10 step 4); notify.py registers send_slack in CHANNELS via lazy import (sys.path-guarded), degrades to warn+no-op when token/channels/module absent, and on actual DELIVERY failure falls OPEN to send_file (§5), telegram sender byte-untouched (§9). ORCH re-ran FULL suite in worktree (subagent): pytest 822 passed / 1 failed, node hooks/run-tests.js 36/0. The one failure (test_sync_from_pack.py::test_every_framework_owned_path_exists_in_the_pack) is the KNOWN ORCH-attributed manifest state, NOT a TASK-015 regression: proven by running it on bare master where it fails identically with a SUPERSET list of 10 files INCLUDING slack_notify.py + test_slack_notify.py — on the branch those two exist, so the branch's missing set is master's-minus-two and TASK-015 partially HEALS the guard (4f106a3 pre-registered the whole wave's scripts+tests in framework_owned at once). Remaining 8 missing files belong to future TOWER tasks (016/017/018); not subject to the per-branch full-suite-green merge gate per plan v4.3 orchestrator_notes. Zero TASK-015-attributable failures. Merged --no-ff, Status→done, branch + wt-s5 worktree removed. No Depends_On chain waits on TASK-015 (Depends_On: —); TASK-018 lists it among five deps (013,014,015,016,017) — not yet unlocked (013/014 still in flight, 016/017 undispatched). | yes | 2026-08-26T06:45:00Z |
| TASK-012 | CX | approved | Territory CLEAN: 3-dot diff master...task/TASK-012-cx = exactly the 3 Owned_Paths (scripts/supervisor.py, tests/test_supervisor.py, autopilot.json), single CX commit eb8f04e, zero outside; no PLAN.md commit on branch, no frontmatter/other-block edits. autopilot.json adds EXACTLY ONE key (`max_dispatch_failures: 2`) — nothing else in the registry/review_cmd file changed. §0 HONOURED — detection was NOT rebuilt: launch_shell_bg→inflight→reap_inflight→_notify_if_builder_unreachable reused and only extended. Spec verified §3 B1/B2/B3 + §1 L2/L3/L4 + §5 + §6 — all 7 ACs map to spec text. B1: `dispatch_failures` dict keyed by UNIT in RuntimeState (mirrors rework/stale/conflict counts), PERSISTED (save dumps __dict__, load filters known fields → round-trips); decide() parks at `>= max_dispatch_failures`; reap increments on nonzero, resets to 0 on rc==0; ceiling P2 fires exactly ONCE (`failures == ceiling`) and parking suppresses further dispatch→reap→notify (never one-per-tick). B2: reap_inflight gained `wait_seconds`; main() reaps after execute() when `--once and not --dry-run` (3.0s) before save/break — closes the single-tick hole that hid the live incident. B3: notice widened to exit code + actual command + BOTH candidates (unreachable CLI AND local dispatch-precondition/stale worktree) + AUTOPILOT_LOG.md pointer (honest — the DISPATCH_COMMAND log line is really written there); T1 Watchtower retained; no single-cause assertion. HEALTHY DISPATCH UNCHANGED (verified, not just untested): busy_units is vestigial (def/set/new-pop, ZERO reads) so the added pop can't change decisions; rc==0 path only resets counter, no notify; _notify has one caller (rc≠0 branch) so dropping its old rc==0 guard is inert. EMPIRICAL (subagent, worktree): branch pytest 723/0 (717 pre-TASK-011 base + 6 new) + node 36/0 — matches Test_Evidence. §6 GRADED / L4 PROVEN: pre-fix master supervisor.py under the 4 new tests → ALL 4 FAIL (incl. `ValueError: too many values to unpack` from the old 2-tuple reap, and the --once test's `assert 'exited 7'`); the --once test drives a real subprocess `sys.exit(7)` through `main(["--once"])` and asserts the failure surfaces (silent without the fix); worktree restored clean. Cross-package guard: integrated master → pytest 730/0 (724 + 6), node 36/0. Non-blocking (no rework, first real occurrence on pre-existing files): no c8b9872 ls/find preflight evidence recorded though all three paths pre-existed — record it next time. Merged --no-ff (e3942a7), branch deleted. CLOSES the L2 DISPATCH RESILIENCE wave (TASK-011 + TASK-012 both first-pass done). TASK-012 grants (supervisor.py, autopilot.json) remain LIVE in hooks/lib.js — removal deferred to Alister's dedicated grant step; hooks/lib.js untouched. | yes | 2026-08-16T22:05:00Z |
| TASK-013 | GB | approved | Territory CLEAN: net diff master...task/TASK-013-gb = exactly the 3 Owned_Paths (scripts/commands.py NEW, scripts/tg_commands.py shim, tests/test_commands.py NEW); tg_listener.py NOT in diff (SLACK §9 honoured); GB feature commit 3ef169b touched zero PLAN.md and no sync-manifest.json; the only two PLAN.md edits on-branch are ORCH's own master-merge commits (f9c1642, aa8d617) carrying the sync-manifest saga fixes forward — the manifest block/unblock cycles were pre-review ORCH-side test-guard bugs (36b53de), NOT review rework, so this is a genuine first-pass. c8b9872 preflight present (04:03:58Z, all three paths). Spec verified against text: SLACK §5 (commands.py = "the shared command-validation module"; tg_commands "a thin shim, its tests stay green") + TOWER H1 ("same handler path commands.py exposes") + §1 P2 ("unknown → rejected, never guessed"). commands.py owns a CLOSED VOCABULARY frozenset with one validate() entry point returning (ok, {command,args}) | (False, reason); unknown → _reject_unknown "never guessed", typos NOT rewritten to nearest command, every command's malformed/extra-text/wrong-type/unknown-dict-key path rejected — all discriminating in test_commands.py (test_unknown_tokens_rejected, test_typo_is_not_rewritten_to_nearest_command, test_no_arg_rejects_extra_text, test_*_rejects_*, test_args_wrong_type, test_unknown_dict_keys_rejected_never_guessed). FULL prior public surface preserved: enumerated every tgc.* attribute referenced by control.py/maintenance.py/supervisor.py/test_tg_commands.py — all resolve; validation parsers re-exported from commands, git_pull/git_commit_and_push/git_commit_and_push_detailed + render_* stay physically in tg_commands (per ATLAS coupling); dropped-only names (e.g. AMEND_RE regex) referenced by nobody. ORCH re-ran FULL suites (subagent) in GB's worktree AND on integrated master post-merge: 881 pytest / 36 node, 0 failures BOTH; manifest guard tests/test_sync_from_pack.py 49/0 green post-merge (commands.py now registered AND present). Merged --no-ff (c3c70b0) with scripts/commands.py + tests/test_commands.py registered in sync-manifest.json framework_owned IN THE SAME merge commit (merge-time-only rule, 36b53de/9f32fa0). Unlocks TASK-016 (S5 — reassigned from GB per e9bb287 during GB's weekly rate-limit outage) and TASK-017 (CX); both Depends_On: TASK-013 only. Branch deleted. | yes | 2026-08-26T08:10:00Z |
| TASK-014 | CX | approved | Territory CLEAN: net diff master...task/TASK-014-cx = exactly the 2 Owned_Paths (scripts/tower_sync.py NEW, tests/test_tower_sync.py NEW), single CX commit fca1b3a, zero files outside; no PLAN.md commit on branch (git log -p -- PLAN.md empty), no frontmatter/other-block edits; autopilot.json untouched (ORCH added tower._token_env=DEVTEAM_TOWER_TOKEN in an earlier planning commit). Spec verified against TOWER §1/H2/H3/H4/H5 text itself (not the builder summary): build_snapshot() emits schema v1 with EVERY §1 field present (schema/project_id/ts/pack_version, supervisor{mode,autonomy_level,tick,stop_file}, wave totals, tasks[], builders[], review_queue[], usage, recent_events[]); module-underivable fields (mode/tick/wave_started_at/prev_wave_minutes/pack_version/rework_count) emitted as null and documented in the module docstring, never invented (H2). tasks[] built ONLY via validate_plan.parse_tasks — grep confirms NO second PLAN.md parser (no re./compile/### splitting). usage read ONLY via usage_probe.load_cache — grep confirms probe() (the live network call) is never invoked (H5: broken probe can't stall a tick). Transport stdlib-only (urllib.request), 10s timeout: POST {url}/ingest with Authorization: Bearer sourced from env var NAMED BY tower._token_env (os.environ.get) — grep confirms no hardcoded/tracked token literal anywhere (H4); GET {url}/queue/{project_id} materialises each pending command as one JSON file into .devteam/inbox/ via atomic tmp+replace, then DELETEs each acked entry. Fail-open (H5) EXERCISED in tests, not just read: tower.enabled=false or url empty → exact silent no-op (test_disabled_is_exact_noop asserts transport never called); unreachable/timeout/non-200 → exactly ONE "[tower] warning:" line to stderr and clean return, never raises (test_failure_warns_once asserts count==1). No live network in tests — transport stubbed + usage_probe.load_cache monkeypatched. ORCH re-ran FULL suites (subagent, worktree wt-review-014): python -m pytest 743 passed / 1 failed (test_tower_sync 4/4 pass), node hooks/run-tests.js 36/0. The ONE failure is the KNOWN, EXPECTED sync-manifest registration gap (test_sync_from_pack::test_every_shipped_test_file_is_registered — tests/test_tower_sync.py not yet in framework_owned), resolved by ORCH in THIS merge commit per the merge-time-only registration rule (9f32fa0/36b53de) by adding scripts/tower_sync.py + tests/test_tower_sync.py to sync-manifest.json framework_owned. Post-merge master re-run confirms manifest failure gone and nothing else broke. Merged --no-ff to master, Status→done, branch deleted. Satisfies one more TASK-018 Depends_On (single-owner supervisor.py integration, needs ALL of 013–017). TASK-014 firewall grants remain LIVE in hooks/lib.js — removal deferred to Alister's dedicated grant step; hooks/lib.js untouched. | yes | 2026-08-26T07:17:58Z |
| TASK-017 | CX | approved | Territory CLEAN: net diff master...task/TASK-017-cx = exactly the 2 Owned_Paths (scripts/inbox.py NEW, tests/test_inbox.py NEW) across both CX commits (b539726 feature, ef778a6 queue-compat follow-up), zero files outside; scripts/tower_sync.py zero diff; no PLAN.md commit on branch beyond CX's own TASK-017 block (git log -p -- PLAN.md: only status/criteria/notes on its block, no frontmatter/other-block edits). CX's Test_Evidence (887 passed/1 failed) was STALE — its branch predated TASK-014's merge (38c36b8), which registered tower_sync in sync-manifest.json; the sole reported failure (test_sync_from_pack::test_every_shipped_test_file_is_registered) was that already-resolved gap. ORCH merged current master into the branch and re-ran a LIVE full suite: python -m pytest 893 passed / 0 failed (manifest guard now green, test_inbox 8/8), node hooks/run-tests.js 36/0. All 7 ACs verified against TOWER §1 P2/H1/H5 text, empirically not by summary: H1 — drain_inbox validates every command through commands.validate() (the shared module), grep confirms NO second command-vocabulary validator inside inbox.py; the envelope structural check is P2-schema-level only. §1 P2 — required keys id/issued_at/source/actor/command/args enforced with type checks; malformed JSON / unknown command / bad schema / duplicate id → file MOVED (path.replace) to .devteam/inbox/rejected/ with a .reason sidecar, never deleted, never guessed (tested: test_bad_json_unknown_commands_and_bad_schema_are_rejected_with_reasons). Two-phase list/ack — drain returns validated commands but leaves the file on disk (test_valid_command...survives_drain asserts path.exists() after drain, "draining is not acknowledgement" = the crash-between-drain-and-handling property); ack() writes the consumed-id ledger BEFORE unlinking. Duplicate ids — second occurrence rejected as duplicate, and the ledger is DURABLE across process restart: backed by on-disk .consumed_ids.json re-loaded each drain (NOT an in-memory set), proven by test_ack_is_the_second_phase_and_a_repeated_id_is_rejected (fresh drain_inbox call rejects the dup) + test_two_files_with_the_same_id_reject_the_second. Fail-open H5 — per-file read/parse error → warn + skip, drain_inbox never raises into the tick (tested via monkeypatched read_text OSError → returns [] + warns); ack() also swallows and warns. Output shape — validated dict carries cmd/args/chat_id/update_id/raw matching the existing Telegram queue item surface, asserted in test_text_command_matches_the_existing_telegram_queue_item_shape against the real shape. Merged --no-ff to master (04355ea) with scripts/inbox.py + tests/test_inbox.py registered in sync-manifest.json framework_owned IN THE SAME merge commit (merge-time-only rule); post-merge master re-run confirmed green. Status→done, branch deleted. Closes the final module dependency: TASK-018 (single-owner supervisor.py integration) now awaits ONLY TASK-016 (S5). TASK-017 firewall grants (scripts/inbox.py) remain LIVE in hooks/lib.js — removal deferred to ORCH's dedicated grant step; hooks/lib.js untouched. | yes | 2026-08-26T08:26:00Z |
