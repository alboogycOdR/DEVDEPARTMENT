# Dossier — TASK-018 · supervisor.py integration (the wave's single-owner closer)

**Brief:** The only task that edits supervisor.py, after all five modules are merged — the proven ATLAS-A5 pattern. Four wirings, each thin because the modules did the work: tower tick sync, inbox drain before decide(), Slack listener startup, and the unified command-queue drain.

**Spec:** TOWER §1 P1+P2, H1/H4/H5; SLACK §5 (listener wiring + `_drain_command_queue` rename), §9. Your task block lists all four wirings with their constraints — this dossier adds the how.

**Intended approach:**
1. **Tower tick (P1):** inside the tick, after the existing snapshot/board work, `tower_sync.sync_tick(repo, cfg)` when `tower.enabled`. Fail-open is already inside the module; your job is placement + making sure a tower exception (there should be none, but belt-and-braces) cannot kill a tick — ONE warning line max (H5). Per H4 the push and queue-pull are one round-trip pair inside the same tick.
2. **Inbox (P2):** `inbox.drain_inbox` BEFORE `decide()` (spec wording is exact), routed through the SAME action-handler path the tg queue uses — you are wiring a pass-through, not writing handlers. Honour the two-phase ack: ack each command only after its handler ran.
3. **Slack listener:** start `SlackListener` alongside `TelegramListener` when `"slack"` in notify_channels and env vars present; missing env → warning + not started (exactly the tg posture). **Telegram start logic byte-preserved** (§9).
4. **The rename (§5):** `_drain_tg_queue` → `_drain_command_queue`, draining both listeners' queues through one path. tg_listener.py is out of territory — the rename is supervisor-side only.

**Standing lessons that bind you (all earned in this repo):**
- Reuse the existing reap/notify machinery; never rebuild what exists (L2 spec §0 — a prior ORCH misreading is recorded there precisely so nobody repeats it).
- **Byte-identical-when-disabled** is the graded criterion (ATLAS A5 precedent): with tower+slack+inbox all disabled/absent, a tick must behave identically to pre-wave master — write the test that proves it, don't argue it structurally (TASK-006's rework came from arguing structurally).
- DEFAULT_CONFIG gains tower/slack keys mirroring the template ORCH committed — keep them in exact sync with autopilot.json's blocks.

**Verification:** full pytest + node suites; the disabled-path parity test; one fail-open test per wiring (tower down, inbox malformed file, slack env absent).

**Territory note:** you run ALONE (Wave C) — everything else is merged by the time you start. scripts/supervisor.py grant lives in hooks/lib.js, removed at done. Read every module's actual merged surface before wiring — do not code against this dossier's function names if the merged reality differs.

## Work Log

- [2026-08-26T08:30:00Z] [S5] Read all four merged upstream modules (tower_sync.py, inbox.py, slack_listener.py, tg_listener.py) plus commands.py and the full current supervisor.py (1132 lines) before touching anything, per the dossier's "do not code against function names if the merged reality differs" warning.
- **Key deviation from dossier's literal wording, and why:** the dossier/spec say `_drain_tg_queue` → `_drain_command_queue`. The merged reality is that the function is named `drain_tg_queue` (public, no underscore) — AND `tests/test_supervisor_telegram.py` (outside my Owned_Paths, cannot be edited) imports and calls it directly with its exact signature `drain_tg_queue(q, repo, cfg, state, wave_event, now, token=...)` in ~15 places. Renaming/removing it would break that test file with no way for me to fix it. Resolution: added a NEW `drain_command_queue(queues: list[Queue], ...)` that drains multiple queues (tg + slack) through the same per-item handler — this is the real "one drain path for both queues" the spec wants — and kept `drain_tg_queue` as a thin one-line backward-compatible wrapper (`drain_command_queue([q], ...)`). Functionally identical outcome to the spec's intent; only the old public name survives as an alias. Not treating this as SPEC_AMBIGUITY/blocking since the functional requirement (both queues, one handler) is fully met and the alternative (breaking an out-of-territory test file) is strictly worse.
- Implemented all four wirings in scripts/supervisor.py: (1) `tower_sync.sync_tick(repo, cfg, state={"mode":..., "tick": ticks})` called every non-dry-run tick right after the existing board-publish block, wrapped in a belt-and-braces try/except (sync_tick itself already never raises — H5 fail-open lives inside the module; my wrapper is defense-in-depth per the dossier). (2) `drain_inbox_commands()` (P2): drains `.devteam/inbox/` via `inbox.drain_inbox()` (already validated through commands.py — H1) BEFORE `decide()`, routes each item through the SAME `_process_tg_command()` handler tg/slack use, and calls `inbox.ack()` only after that item's handler returns without raising (two-phase — a handler exception leaves the file for retry next tick, verified by a dedicated test). (3) `_start_slack_listener(cfg)`: starts `SlackListener` when `"slack"` in `notify_channels` AND both `DEVTEAM_SLACK_APP_TOKEN`/`DEVTEAM_SLACK_TOKEN` are set; missing env → one stderr warning, not started (tg-style); `_start_tg_listener`'s own logic is byte-unchanged (§9), verified by a regression test. (4) DEFAULT_CONFIG gained `tower`/`slack` blocks mirroring autopilot.json's template exactly (functional keys; `_comment` doc-only fields excluded, consistent with how other DEFAULT_CONFIG blocks already work).
- Added 19 new tests to tests/test_supervisor.py (my only other Owned_Path) covering: DEFAULT_CONFIG/load_config tower+slack merge, drain_command_queue draining both queues through one handler + one-bad-command-doesn't-block-the-other + the drain_tg_queue alias, inbox drain (stop applies+acks, approve→REVIEW_TG, absent dir no-op, drain-failure fail-open, handler-exception leaves file unacked for retry), tower tick (called with repo/cfg/state, failure→exactly one `[tower]` warning line tick continues, disabled→zero tower output), Slack listener startup (started when configured+env present, not started when absent from notify_channels, not started missing env with warning, telegram start logic unchanged when slack also configured), and the ATLAS-A5-style byte-identical-when-disabled test (tower+slack+inbox all disabled/absent → zero trace of any of the three wirings in a real `main(["--once", ...])` run).
- Test evidence: `pytest tests/test_supervisor.py -q` → 47 passed (28 pre-existing + 19 new, zero regressions). `node hooks/run-tests.js` (run from the main checkout per established convention) → 36 passed, 0 failed. Full `pytest -q` baseline BEFORE my changes was 912 passed (confirmed); full-suite rerun after my changes in progress, will append final count before needs_review.
- `git diff --stat` vs the branch point is exactly scripts/supervisor.py + tests/test_supervisor.py — no other files touched.
- Self-checked all 7 acceptance criteria against the diff: tower push+pull wiring ✓, inbox drain-before-decide with same handler path + two-phase ack ✓, SlackListener startup with tg-style fail-open + tg logic unchanged ✓, drain_command_queue as the one-path-for-both-queues (with drain_tg_queue alias for the out-of-territory test file) ✓, byte-identical-when-disabled test ✓, DEFAULT_CONFIG tower/slack keys ✓, regression tests for every wiring incl. fail-open paths + full suites green ✓.
- [2026-08-26T11:05:00Z] [S5] Final full-suite rerun (foreground, no background timeout this time): `python -m pytest -q` → **931 passed, 0 failed** in 146.19s (up from the 912 pre-change baseline; +19 new supervisor tests, zero regressions elsewhere — matches the count of new tests added). `node hooks/run-tests.js` (run from the main checkout) → 36 passed, 0 failed, unchanged. `git diff --stat master...HEAD` reconfirmed exactly scripts/supervisor.py + tests/test_supervisor.py (455 lines changed total), nothing else. autopilot.json's committed `tower`/`slack` template blocks (lines 129-142) compared field-for-field against DEFAULT_CONFIG's new blocks — identical on every functional key, `_comment` excluded as documented. Handing off to needs_review via PLAN.md + `scripts/plan_commit.sh`.
