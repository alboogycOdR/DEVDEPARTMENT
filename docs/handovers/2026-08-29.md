# Handover — 2026-08-29

## State: YELLOW
Master is green (944 pytest / 36 node, `validate_plan.py` legal) and pushed. YELLOW only because a dispatched builder (S5, TASK-022) is running unattended in its own detached window as this is written — its outcome isn't known yet.

## What changed this session
- Reverted a live production-config regression: a concurrent session had committed `tower.enabled: true` (+ real URL/project_id) directly into `autopilot.json` (commit `aa1da4a`) to push DEVDEPARTMENT's own first Tower snapshot. That file is simultaneously DEVDEPARTMENT's own config **and** the shipped pack template every onboarded project inherits via `sync_from_pack.py`'s add-only-keys merge — so the flip leaked toward every other project and broke `tests/test_sync_from_pack.py::TestPackTemplateShipsSafeDefaults` (full suite RED on master). Reverted in `6df0fa7` back to `enabled:false,url:"",project_id:""`; the underlying push itself was NOT undone — DEVDEPARTMENT's row is still live on Tower with a real snapshot (`2026-08-29T04:51:45Z` server-side), only the tracked-template leak was reverted.
- Filed **TASK-022** (S5, high priority, `Owned_Paths: scripts/supervisor.py, tests/test_supervisor.py`): build a genuinely optional `autopilot.local.json` (gitignored) that `load_config()` merges over the tracked `autopilot.json`, add-only-keys, silent no-op when absent — so DEVDEPARTMENT (or any project) can configure itself locally without mutating the shared template. This is the second time this exact mistake has happened (the first was with ATLAS, per that test's own docstring) — it's a structural gap, not a one-off slip, so a task exists now instead of a third revert next time.
- Refreshed `orchestrator_notes` (it had grown into a multi-thousand-word accretion of every prior scan) down to a short current-state note; bumped `plan_version` to 5.13. Committed as `e9c6cd2` and pushed.
- Redispatched S5 onto TASK-022 — first attempt (`dispatch.ps1 -Unit S5 -Task TASK-022`) failed immediately with `A parameter cannot be found that matches parameter name 'Unit'` (the script only accepts `-Builder`, no `-Task` flag at all — it always claims the next eligible task itself). Fixed the invocation to `dispatch.ps1 -Builder S5`; it launched cleanly in its own detached window (`.devteam/launch/S5-20260829-070045.log`).

## Half-done / in-flight
- **TASK-022** (this repo): dispatched, running unattended in its own window as of `.devteam/launch/S5-20260829-070045.*`. Not yet checked for completion. Run `/devteam-status` to see where it landed — it may already be at `needs_review`.
- **Tower repo TASK-127** (card→drill-down navigation): reviewed the diff and territory (clean — only `src/**`, exactly the branch's Owned_Paths), full-suite test run was **dispatched to a subagent and never confirmed complete** before this handover was written (agent id `a6827e443575e5a57`, output file `...\tasks\a6827e443575e5a57.output`, was still empty at 0 lines the last time it was checked). **Do not merge TASK-127 until that result is actually seen** — the diff review is not a substitute for the real full-suite run per protocol.
- Once TASK-127 is approved and merged, the plan (already stated to the user) is a **single combined clawsrv redeploy** bundling TASK-124 (schema), TASK-125 (deploy secrets, already deployed once), TASK-126 (light theme, merged but not yet redeployed), TASK-128 (SPA-fallback fix, merged but not yet redeployed), and TASK-127 (nav) together — then verify live navigation by actually clicking through the deployed site (not just curl/status-code checks) before telling the user it's done. The user has twice been told things worked ("deep links resolve correctly", "reviewing TASK-126 now") that turned out to be false or premature — the redeploy report needs to be based on what was actually clicked, not inferred from source review.

## Next 3 steps — concrete, in order
1. Run `/devteam-status` in this repo (DEVDEPARTMENT) to check whether TASK-022 finished, and review it if it's at `needs_review` (full territory diff, re-run tests yourself via a subagent, check acceptance criteria against the task block above).
2. In the `tower` repo: retrieve the TASK-127 test-run subagent's actual result (agent id above, or just re-run `npm run build && npm run lint && npm run test && python -m pytest -q && node hooks/run-tests.js` in `C:\CLAUDECODE_kingdom.work\wt-codex-tower` — that worktree is already checked out to the TASK-127 branch, commit `f0d5d51`). If green, record the verdict in tower's `REVIEW.md`, merge `task/TASK-127-cx --no-ff` to `master`, flip `Status: done`, delete the branch.
3. Redeploy clawsrv from tower `master` (bundling TASK-124/125/126/127/128) and verify live navigation by clicking through — home dashboard card click → project drill-down → workshop link → back — on the actual deployed site, not just status codes.

## Traps discovered
- `scripts/dispatch.ps1` takes `-Builder <unit>` only — there is no `-Task` parameter. It always claims whatever task is next-eligible for that unit in PLAN.md; you cannot target a specific task ID by flag. Passing `-Unit` or `-Task` fails immediately with `ParentContainsErrorRecordException`.
- `autopilot.json` in **this repo specifically** is both DEVDEPARTMENT's live project config and the pack's shipped template (every onboarded project inherits it via `sync_from_pack.py`'s add-only-keys merge). Never flip a value here "just to test something locally" — it leaks outward. This is exactly what TASK-022 is meant to fix; until it lands, treat this file as write-once-then-immediately-revert for any local experiment.
- Tower currently holds two DB rows for this project: lowercase `devdepartment` (registered, never pushed anything) and uppercase `DEVDEPARTMENT` (the one actually receiving snapshots). Harmless duplication, not urgent — worth revoking the unused lowercase row next time you're on clawsrv.

## Don't touch
- `autopilot.json`'s `tower` block is deliberately `{enabled:false, url:"", project_id:""}` right now — that's the safe-defaults guard passing, not an oversight. Don't flip it again by hand; wait for TASK-022's `autopilot.local.json` mechanism.
